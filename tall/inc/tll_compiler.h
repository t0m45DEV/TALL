#ifndef _TLL_COMPILER_H
#define _TLL_COMPILER_H

/**
 * Parse the given source_code, checking for compile errors, and generates bytecode for the TALL VM.
 */
void compile_code(const char* source_code);

#endif
