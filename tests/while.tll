const limit : int = 10;
var n : int = 0;

{
    var i : int = 0;

    while (i < 10)
    {
        if (i != 5)
        {
            n = n + i;
        }
        i++;
    }
    n = n + 5;
}

return n;

