
func fibonacci(n : int) -> int
{
    if (n < 2)
    {
        return n;
    }
    else
    {
        return fibonacci(n - 2) + fibonacci(n - 1);
    }
}

func main(null) -> int
{
    var start : float = clock();
    fibonacci(3);
    print(clock() - start);
    return 0;
}

