
func main(null) -> int
{
    const limit : int = 10;
    var n : int = 0;

    for (var i : int = 0; i < limit; i++)
    {
        if (i != 5)
        {
            n += i;
        }
    }
    n = n + 5;

    return n;
}

